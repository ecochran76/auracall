# Execution Plan | 0001-2026-04-14

State: OPEN
Lane: P01

## Scope

- use the canonical plan set as the active planning authority
- stop further plan-structure churn unless a genuinely new governing track appears
- transition from planning migration work into product execution
- define the next concrete product lane under the existing canonical plans

## Current State

- mixed-provider and operator-control live proof is materially sufficient for
  the current hardening checkpoint
- runtime/readback hardening is materially sufficient and has been consolidated
  into compact contract notes
- the bounded `0004` operator-facing claimant/readback hardening sub-lane is
  now materially complete enough to park unless a new concrete mismatch is
  demonstrated
- the narrower `api serve` server-local-runner ownership/readback checkpoint is
  also materially complete enough to park unless a fresh mismatch is
  reproduced
- canonical policy now lives under `docs/dev/policies/`
- canonical bounded plan location now exists under `docs/dev/plans/`
- `pnpm run plans:audit` now deterministically ranks loose plan candidates
- current migration audit result is:
  - `keep = 18`
  - `merge = 0`
  - `retire = 0`
- canonical active plan set now includes:
  - `0001-2026-04-14-execution.md`
  - `0002-2026-04-14-task-run-spec.md`
  - `0003-2026-04-14-team-run-data-model.md`
  - `0004-2026-04-14-team-service-execution.md`
  - `0005-2026-04-14-durable-state-account-mirroring.md`
  - `0006-2026-04-14-team-config-boundary.md`
  - `0007-2026-04-14-config-model-refactor.md`
  - `0008-2026-04-14-browser-profile-family-refactor.md`
  - `0009-2026-04-14-agent-config-boundary.md`
  - `0010-2026-04-14-service-volatility-chatgpt.md`
  - `0011-2026-04-14-browser-service-refactor-roadmap.md`
  - `0012-2026-04-14-service-volatility-refactor.md`
  - `0013-2026-04-14-gemini-completion.md`
  - `0014-2026-04-14-browser-service-reattach-reliability.md`
  - `0015-2026-04-15-team-run-review-ledger.md`
  - `0016-2026-04-15-passive-provider-observations.md`
  - `0017-2026-04-16-runtime-inspection-service-state-probe.md`
  - `0018-2026-04-17-service-state-quality-follow-up.md`
- the reviewed loose-plan backlog is now exhausted
- the review-ledger checkpoint is complete
- the service-state probing and quality lanes are complete:
  - `0017-2026-04-16-runtime-inspection-service-state-probe.md`
  - `0018-2026-04-17-service-state-quality-follow-up.md`
- the next unresolved execution lane is the internal team/task execution
  cluster under:
  - `0002-2026-04-14-task-run-spec.md`
  - `0003-2026-04-14-team-run-data-model.md`
  - `0004-2026-04-14-team-service-execution.md`
- current roadmap classification is:
  - primary active implementation lane:
    - service/runner orchestration beyond the current single-host bounded
      local-runner bridge
  - active supporting authorities for that lane:
    - `0002-2026-04-14-task-run-spec.md`
    - `0003-2026-04-14-team-run-data-model.md`
    - `0004-2026-04-14-team-service-execution.md`
    - `0006-2026-04-14-team-config-boundary.md`
    - `0009-2026-04-14-agent-config-boundary.md`
  - maintenance-only unless a concrete mismatch is reproduced:
    - `0007-2026-04-14-config-model-refactor.md`
    - `0008-2026-04-14-browser-profile-family-refactor.md`
    - `0010-2026-04-14-service-volatility-chatgpt.md`
    - `0011-2026-04-14-browser-service-refactor-roadmap.md`
    - `0012-2026-04-14-service-volatility-refactor.md`
    - `0014-2026-04-14-browser-service-reattach-reliability.md`
  - browser-service maintenance exception closed on 2026-04-21:
    - `0021-2026-04-21-browser-operation-dispatcher.md`
    - `0022-2026-04-21-provider-selector-diagnosis-hardening.md`
    - result: default managed-profile account checks now have dispatcher
      ownership and surface-aware selector diagnosis
    - no active browser reliability exception remains unless a new concrete
      mismatch is reproduced
  - side-track and not primary sequencing authority:
    - `0013-2026-04-14-gemini-completion.md`
  - closed checkpoints retained for history:
    - `0005-2026-04-14-durable-state-account-mirroring.md`
    - `0015-2026-04-15-team-run-review-ledger.md`
    - `0016-2026-04-15-passive-provider-observations.md`
    - `0017-2026-04-16-runtime-inspection-service-state-probe.md`
    - `0018-2026-04-17-service-state-quality-follow-up.md`

## Non-Goals

- do not perform a broad semantic rewrite of roadmap lanes
- do not normalize all legacy plans in one slice
- do not change runtime or operator behavior
- do not convert `RUNBOOK.md` into the stricter dated turn-log shape in this
  slice

## Acceptance Criteria

- the execution board reflects the current canonical state accurately
- `ROADMAP.md`, `RUNBOOK.md`, and the canonical plan set stay aligned
- the next product lane is explicit
- the deterministic plan audit remains green without more structural migration

## Definition Of Done

- the completed planning-migration phase is recorded as finished
- the execution board no longer points at already-completed archive work
- the next product lane is explicit and sequenced against the roadmap
- doc hygiene notes are recorded in the journal and fixes log
- targeted verification passes

## Next Slice

- keep the strict planning audit green while returning to product work
- the first concrete task / run-spec shape is now defined in `0002-2026-04-14-task-run-spec.md`
- the concrete `teamRun` v1 execution contract is now defined in `0003-2026-04-14-team-run-data-model.md`
- the first implementation slice in `0004-2026-04-14-team-service-execution.md` is now partially realized in code:
  - validated `taskRunSpec` records are now persisted under the AuraCall home dir
  - config/resolved-team bridge execution now persists `taskRunSpec` before runtime run creation
  - one sequential `teamRun` still projects into deterministic initial `step` and `sharedState` runtime records
- the bounded internal inspection/readback seam for persisted `taskRunSpec -> teamRun` linkage is now live on response readback and recovery detail
- the bounded CLI write surface for team execution is now live as
  `auracall teams run`:
  - it persists one bounded `taskRunSpec`
  - it projects one sequential `teamRun`
  - it now executes through one persisted local runner with runner-backed
    lease ownership and configured account-affinity projection
  - that bounded local runner now keeps its heartbeat/liveness fresh while
    active multi-step CLI work is still draining
- bounded public team execution writes are now live on HTTP and MCP surfaces
  through compact create requests that build internal `TaskRunSpec` records
- the narrow internal debug/inspection command is now live as `auracall teams inspect`
- the first bounded public read-only team inspection surface is now live as `GET /v1/team-runs/inspect`
  - current lookup keys:
    - `taskRunSpecId`
    - `teamRunId`
    - `runtimeRunId`
- the first bounded public read-only runtime queue surface is now live as `GET /v1/runtime-runs/inspect`
  - required lookup keys:
    - `runId`
    - `runtimeRunId`
    - `teamRunId`
    - `taskRunSpecId`
  - optional runner evaluation:
    - `runnerId`
- configured service-account affinity is now live across:
  - `api serve` runner metadata
  - runtime queue projection
  - runtime inspection readback
  - service-host local claim
  - targeted-drain skip diagnostics
- the bounded `api serve` operator smoke for the documented
  read-only/account-affinity posture passed on 2026-04-15
- the completed team-run review-ledger checkpoint remains the prerequisite
  foundation:
  - read-only ledger projection
  - `auracall teams review`
  - stored provider reference enrichment
  - durable failure-derived hard-stop observations
- the runtime-inspection service-state probe lane is now complete:
  - `0017-2026-04-16-runtime-inspection-service-state-probe.md`
  - `0018-2026-04-17-service-state-quality-follow-up.md`
- provider passive/live service state is now explicitly a closed observability
  seam for the current phase:
  - provider/browser adapters own passive observation emission and live probe
    evidence
  - runtime inspection exposes that state as read-only inspection only
  - runner control remains lease-owned and executor-return-driven rather than
    reacting generically to passive/live provider-state transitions
- the internal team/task execution MVP is now live:
  - one persisted `taskRunSpec`
  - one sequential `teamRun`
  - deterministic initial `step` and `sharedState` records
  - one bounded CLI execution entrypoint
- the next finite implementation lane is service/runner orchestration beyond
  the current single-host bounded local-runner bridge:
  - keep the current team/task projection semantics unchanged
  - keep public HTTP/MCP team execution writes paused
  - keep tightening service-mode ownership beyond the current bounded local
    runner before widening execution surfaces
- the next implementation checkpoint should design and land the smallest
  service/runner ownership increment that preserves the existing
  `taskRunSpec -> teamRun -> step/sharedState` contract
- first ownership increment completed:
  - `ExecutionServiceHost` now owns local runner registration, heartbeat
    refresh, and shutdown/stale marking
  - HTTP `api serve` keeps timer/status responsibilities but delegates runner
    lifecycle writes to the service host
- second ownership increment completed:
  - `ExecutionServiceHost` now owns serializing queued drain-until-idle calls
    for one host instance
  - HTTP `api serve` keeps background timer, pause/resume, and status
    projection responsibilities
- third ownership increment completed:
  - `ExecutionServiceHost` now owns route-neutral run-control dispatch for
    cancel, resume-human-escalation, and targeted drain actions
  - HTTP `api serve` keeps request validation, transport error mapping,
    background timer/pause/resume, and status projection responsibilities
- fourth ownership increment completed:
  - `ExecutionServiceHost` now owns route-neutral operator-control dispatch
    across lease repair, local-action resolution, and run control
  - HTTP `api serve` now only maps validated `POST /status` payloads into the
    service-host control shape before transport error/status projection
- `api serve` remaining ownership boundary reassessed:
  - no remaining direct stored-runtime mutation was found in the HTTP route
    after the fourth ownership increment
  - keep listener lifecycle, background-drain timer state, request parsing,
    transport projection, runner status readback projection, and live
    service-state probe routing in HTTP
  - keep local runner lifecycle mutations, queued drain execution, startup
    recovery drain execution, recovery/local-claim summaries, and
    operator-control mutations in `ExecutionServiceHost`
  - further service-host extraction should require a newly reproduced
    route-neutral runtime mutation still living in HTTP
- closed public write checkpoints:
  - `0019-2026-04-20-public-team-execution-write-surface.md`
    - first bounded HTTP team execution write is live as
      `POST /v1/team-runs`
  - `0023-2026-04-21-mcp-team-run-write-parity.md`
    - MCP write parity is live as `auracall-mcp` tool `team_run`
    - HTTP and MCP both reuse the existing
      `TaskRunSpec -> TeamRun -> TeamRuntimeBridge` chain
- next checkpoint selected:
  - `0024-2026-04-21-taskrunspec-public-contract-reconciliation.md`
  - reconcile the public `TaskRunSpec` contract before accepting arbitrary
    prebuilt spec JSON
  - choose a validation/normalization rule for future public prebuilt-spec
    input
- public prebuilt-spec compatibility is now live through
  `0025-2026-04-21-prebuilt-taskrunspec-acceptance.md`
- service/runner topology was reassessed in
  `0026-2026-04-21-service-runner-topology-reassessment.md`
- read-only runner topology/readiness is live through
  `0027-2026-04-21-runner-topology-readiness-status.md`:
  - `ExecutionServiceHost.summarizeRunnerTopology()`
  - `/status.runnerTopology`
  - local server execution ownership remains scoped to the configured
    `runnerId`
- scheduler-authority preflight is closed through
  `0028-2026-04-21-scheduler-authority-preflight.md`:
  - topology visibility is not assignment authority
  - claim-candidate ordering is not assignment authority
  - `api serve` remains a local runner, not a fleet scheduler
- read-only scheduler-authority evaluation is live through
  `0029-2026-04-21-read-only-scheduler-authority-evaluator.md`:
  - `evaluateStoredExecutionRunSchedulerAuthority(...)`
  - deterministic authority decisions with `mutationAllowed: false`
  - no persistence, scheduler mutation, worker loop, or reassignment behavior
- scheduler-authority runtime inspection readback is live through
  `0030-2026-04-21-runtime-inspection-scheduler-authority.md`:
  - `GET /v1/runtime-runs/inspect?...&authority=scheduler`
  - optional `inspection.schedulerAuthority`
  - no lease acquisition, scheduler mutation, worker loop, or reassignment
- CLI scheduler-authority runtime inspection readback is live through
  `0031-2026-04-21-cli-runtime-inspection-scheduler-authority.md`:
  - `auracall api inspect-run ... --authority scheduler`
  - compact human formatter for the read-only authority posture
  - JSON output preserves the full payload
- scheduler mutation design is closed through
  `0032-2026-04-21-scheduler-mutation-design.md`:
  - first mutation target is `schedulerControl.action = "claim-local-run"`
  - v1 is explicit operator control, gated by the read-only evaluator, and
    scoped to the server-local runner
  - no fleet scheduler, worker loop, non-local assignment, parallel execution,
    new HTTP route, or browser dispatcher bypass
- scheduler local-claim control is closed through
  `0033-2026-04-21-scheduler-local-claim-control.md`:
  - `ExecutionServiceHost` owns `schedulerControl.action = "claim-local-run"`
  - existing `POST /status` maps the payload/result
  - local claims and expired stale/missing-owner reassignments are allowed only
    when scheduler authority selects the server-local runner
  - no fleet scheduler, worker loop, non-local assignment, parallel execution,
    new HTTP route, or browser dispatcher bypass was added
- scheduler roadmap checkpoint is closed through
  `0034-2026-04-21-scheduler-roadmap-checkpoint.md`:
  - do not add a fleet scheduler, background worker loop, non-local assignment,
    or parallel execution yet
  - do not add release-and-reclaim follow-through after `claim-local-run`
  - next bounded implementation target is local-owned active lease execution
    through targeted drain
  - preserve the current service-host, stored-step executor, and
    browser-dispatcher ownership boundaries
- local-owned active lease drain is closed through
  `0035-2026-04-21-local-owned-active-lease-drain.md`:
  - `runControl.action = "drain-run"` can execute runnable work when the active
    lease is already owned by the configured server-local runner
  - foreign active leases still skip
  - existing-lease execution reuses, heartbeats, and releases the same lease
  - no fleet scheduler, worker loop, non-local assignment, new HTTP route, or
    browser dispatcher bypass was added
- scheduler local-control phase closeout is closed through
  `0036-2026-04-21-scheduler-phase-closeout.md`:
  - keep the explicit operator workflow:
    inspect scheduler authority, `claim-local-run`, then targeted `drain-run`
    when immediate execution is desired
  - do not add `claim-and-drain-local-run` or another compound control now
  - scheduler mutation work is paused unless a concrete operator workflow shows
    that the two-step control is too noisy or error-prone
- team-run background-drain parity is closed through
  `0037-2026-04-21-team-run-background-drain-parity.md`:
  - `TeamRuntimeBridge` can create a team runtime without draining it
  - HTTP `POST /v1/team-runs` uses no-drain creation when background drain is
    enabled, then schedules the existing server-owned drain loop
  - synchronous one-request behavior remains when background drain is disabled
  - no public input shape, scheduler mutation, multi-runner assignment, or
    parallel team execution was added
- service/runner roadmap checkpoint is closed through
  `0038-2026-04-21-service-runner-roadmap-checkpoint.md`:
  - broader HTTP/team-runtime validation after Plan 0037 passed
  - no fresh route-neutral runtime mutation was found still owned directly by
    HTTP
  - pause service/runner architecture expansion until a concrete mismatch is
    reproduced
  - next action is integration hygiene across the accumulated worktree before
    selecting another implementation lane
- raw DevTools dispatcher fencing is closed through
  `0039-2026-04-21-raw-devtools-dispatcher-fencing.md`:
  - `browser-tools --port <port>` now acquires a port-scoped dispatcher lock
    instead of bypassing browser-service ownership
  - managed-profile browser-tools commands still prefer the managed browser
    profile dispatcher key when profile/target context is available
  - legacy direct-CDP verification scripts remained follow-up work after this
    slice
- direct CDP script guarding is closed through
  `0040-2026-04-22-direct-cdp-script-guard.md`:
  - legacy direct-CDP scripts under `scripts/` now require an explicit
    `--allow-raw-cdp` flag or `AURACALL_ALLOW_RAW_CDP=1`
  - the flag is consumed before script-specific positional parsing, preserving
    debug-script calling conventions
  - browser-service-owned tooling remains the preferred browser access path
- do not widen into multi-runner execution work yet
- do not reopen response-shape normalization, service-state probing, or broad
  browser/provider maintenance without a newly reproduced mismatch
- no active browser/provider maintenance exception remains after Plans 0021,
  0022, 0039, and 0040 closed

## Audit Notes

- strict planning-contract structure is green
- the canonical plan set is complete for the current governing tracks
- the planning-authority migration phase is finished
- the configured account-affinity checkpoint is complete enough to stop adding
  reporting by default
- the bounded `0004` operator-facing claimant/readback checkpoint is also
  complete enough to stop adding contract locks by default:
  - current claimant semantics are now explicitly locked across:
    - `POST /status`
    - `/v1/runtime-runs/inspect`
    - `/status/recovery/{run_id}`
    - aggregate `/status?recovery=true`
- the validation-first checkpoint is complete:
  - the current `api serve` runner/local-claim/runtime-inspection posture has
    isolated smoke evidence
  - provider passive/live service-state remains adapter-owned observability and
    read-only inspection, not generic runner control
  - service/runner implementation is paused at a coherent checkpoint; the next
    product decision should happen after integration hygiene and review of the
    accumulated changes
